
// const CutInfo = require('./cut');
//
// class IgnoreInfo extends CutInfo {
//   constructor(set, jointInfo) {
//     super(set, jointInfo);
//   }
// }
//
// IgnoreInfo.evaluateSets = (parrelleSets) => {
//   const lenG2 = parrelleSets.filter(s => s.length > 2);
//   const lenE2 = parrelleSets.filter(s => s.length === 2);
//   const lenE1 = parrelleSets.filter(s => s.length === 1);
//
// }
//
// CutInfo.register(IgnoreInfo);
// module.exports = IgnoreInfo;
